spring:
  datasource:
    url: jdbc:h2:mem:ledgerdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
eureka:
  client:
    enabled: false
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
moneybags:
  ledger:
    fake-accounts:
      enabled: true
      accounts:
        10001:
          customer-id: 101
          currency-code: USD
          status: ACTIVE
        20001:
          customer-id: 202
          currency-code: USD
          status: ACTIVE
