$kind: http-request
description: |-
  Generated from product-service/src/main/java/com/example/productservice/controller/ProductController.java.updateProduct

  Request body type: ProductRequest
url: "{{baseUrl}}/api/v1/products/{{id}}"
method: PUT
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "productCode": "SAV-REG",
      "name": "string",
      "productCategory": "DEPOSIT",
      "productType": "SAVINGS",
      "status": "ACTIVE",
      "effectiveFrom": "2026-08-17T10:00:00Z",
      "effectiveTo": "2026-08-17T10:00:00Z",
      "terms": {
        "key": "value"
      }
    }
order: 7000
