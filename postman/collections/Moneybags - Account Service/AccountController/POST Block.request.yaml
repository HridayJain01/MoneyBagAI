$kind: http-request
description: |-
  Generated from services/account-service/src/main/java/com/moneybags/account/controller/AccountController.java.block

  Request body type: StatusChangeRequest
url: "{{baseUrl}}/api/v1/accounts/{{accountId}}/block"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "reason": "Postman example"
    }
order: 16000
