$kind: http-request
description: |-
  Generated from services/branch-employee-service/src/main/java/com/moneybags/branch_employee_service/controller/EmployeeController.java.create

  Request body type: CreateEmployeeRequest
url: "{{baseUrl}}/api/v1/employees"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "userId": 1,
      "employeeCode": "string",
      "dob": "2026-08-17",
      "branchId": 1,
      "designation": "string",
      "reportingManagerId": 1,
      "joiningDate": "2026-08-17"
    }
order: 3000
