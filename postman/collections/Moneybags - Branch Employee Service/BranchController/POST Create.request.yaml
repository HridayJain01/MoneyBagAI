$kind: http-request
description: |-
  Generated from services/branch-employee-service/src/main/java/com/moneybags/branch_employee_service/controller/BranchController.java.create

  Request body type: CreateBranchRequest
url: "{{baseUrl}}/api/v1/branches"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "branchCode": "BR001",
      "name": "string",
      "address": "string",
      "city": "Mumbai",
      "state": "Maharashtra",
      "pincode": "400001",
      "ifscCode": "string"
    }
order: 3000
