$kind: http-request
description: |-
  Generated from services/statement-reporting-service/src/main/java/com/moneybags/statement/Controllers.java.create

  Request body type: ScheduleBody
url: "{{baseUrl}}/api/v1/report-schedules"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "accountId": "a0000000-0000-0000-0000-000000000101",
      "reportType": "ACCOUNT_STATEMENT",
      "outputFormat": "PDF",
      "frequency": "DAILY",
      "nextRunAt": "2026-08-17T10:00:00Z"
    }
order: 1000
