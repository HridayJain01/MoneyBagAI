$kind: http-request
description: Generated from services/identity-service/src/main/java/com/moneybags/identity/controller/UserController.java.removeRole
url: "{{baseUrl}}/api/v1/users/{{userId}}/roles/{{roleId}}"
method: DELETE
headers:
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
order: 10000
