$kind: http-request
description: |-
  Generated from services/identity-service/src/main/java/com/moneybags/identity/controller/UserController.java.create

  Request body type: CreateUserRequest
url: "{{baseUrl}}/api/v1/users"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "username": "opsadmin",
      "email": "asha@example.com",
      "password": "Password@123",
      "fullName": "string",
      "mobile": "9876543210",
      "employeeId": "string",
      "branchCode": "BR001",
      "roles": [
        "OPERATIONS"
      ]
    }
order: 4000
