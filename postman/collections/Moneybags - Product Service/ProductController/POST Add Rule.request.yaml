$kind: http-request
description: |-
  Generated from services/product-service/src/main/java/com/moneybags/product/controller/ProductController.java.addRule

  Request body type: RuleRequest
url: "{{baseUrl}}/api/v1/products/{{productCode}}/rules"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "ruleKey": "string",
      "ruleValue": "string",
      "dataType": "string"
    }
order: 13000
