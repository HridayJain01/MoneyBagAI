$kind: http-request
description: |-
  Generated from services/product-service/src/main/java/com/moneybags/product/controller/ProductController.java.update

  Request body type: UpdateProductRequest
url: "{{baseUrl}}/api/v1/products/{{productCode}}"
method: PATCH
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "productName": "Postman Savings Product",
      "description": "Postman example request",
      "interestRate": 100,
      "minBalance": 100,
      "minOpeningDeposit": 100,
      "maxWithdrawalPerDay": 100,
      "freeTxnPerMonth": 1,
      "minAge": 1,
      "effectiveFrom": "2026-08-17",
      "effectiveTo": "2026-08-17"
    }
order: 7000
