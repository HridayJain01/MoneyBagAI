$kind: http-request
description: |-
  Generated from services/product-service/src/main/java/com/moneybags/product/controller/ProductController.java.create

  Request body type: CreateProductRequest
url: "{{baseUrl}}/api/v1/products"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "productCode": "SAV-REG",
      "productName": "Postman Savings Product",
      "productType": "SAVINGS",
      "description": "Postman example request",
      "currency": "INR",
      "interestRate": 100,
      "minBalance": 100,
      "minOpeningDeposit": 100,
      "maxWithdrawalPerDay": 100,
      "freeTxnPerMonth": 1,
      "tenureMonths": 1,
      "allowsOverdraft": true,
      "requiresFunding": true,
      "minAge": 1,
      "effectiveFrom": "2026-08-17"
    }
order: 6000
