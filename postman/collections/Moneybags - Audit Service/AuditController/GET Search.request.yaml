$kind: http-request
description: Generated from services/audit-service/src/main/java/com/moneybags/audit/controller/AuditController.java.search
url: "{{baseUrl}}/api/v1/audit-events?sourceService=account-service&eventType=ACCOUNT_UPDATED&aggregateType=ACCOUNT&aggregateId=a0000000-0000-0000-0000-000000000101&actorEmployeeId=101&branchCode=BR001&from=2026-08-01&to=2026-08-17&page=0&size=50"
method: GET
headers:
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
order: 2000
