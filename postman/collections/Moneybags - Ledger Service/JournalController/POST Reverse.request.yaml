$kind: http-request
description: |-
  Generated from services/ledger-service/src/main/java/com/moneybags/ledger/controller/JournalController.java.reverse

  Request body type: ReversalRequest
url: "{{baseUrl}}/api/v1/ledger/journals/{{journalId}}/reverse"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "journalReference": "JE-POSTMAN-001",
      "description": "Postman example request",
      "createdBy": "string"
    }
order: 2000
