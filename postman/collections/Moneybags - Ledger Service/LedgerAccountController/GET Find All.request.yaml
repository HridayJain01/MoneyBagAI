$kind: http-request
description: Generated from services/ledger-service/src/main/java/com/moneybags/ledger/controller/LedgerAccountController.java.findAll
url: "{{baseUrl}}/api/v1/ledger/accounts"
method: GET
headers:
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
order: 1000
