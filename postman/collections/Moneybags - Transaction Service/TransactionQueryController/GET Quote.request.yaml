$kind: http-request
description: Generated from services/transaction-service/src/main/java/com/moneybags/transaction/controller/TransactionQueryController.java.quote
url: "{{baseUrl}}/api/v1/transactions/limits/quote?accountId=a0000000-0000-0000-0000-000000000101&transactionType=DEPOSIT&rail=INTERNAL&channel=NEFT&currency=INR&amount=100.00"
method: GET
headers:
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
order: 9000
