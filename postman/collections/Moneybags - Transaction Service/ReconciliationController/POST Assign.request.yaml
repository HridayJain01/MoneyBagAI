$kind: http-request
description: |-
  Generated from services/transaction-service/src/main/java/com/moneybags/transaction/controller/ReconciliationController.java.assign

  Request body type: AssignRequest
url: "{{baseUrl}}/api/v1/reconciliation/exceptions/{{id}}/assign"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "investigatorId": "string"
    }
order: 3000
