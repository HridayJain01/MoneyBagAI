$kind: http-request
description: |-
  Generated from services/transaction-service/src/main/java/com/moneybags/transaction/controller/TransactionCommandController.java.productPurchase

  Request body type: ProductPurchaseRequest
url: "{{baseUrl}}/api/v1/transactions/product-purchases"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: Idempotency-Key
    value: "{{idempotencyKey}}"
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "sourceAccountId": "string",
      "productCode": "SAV-REG",
      "amount": 100,
      "currency": "INR",
      "paymentChannel": "BRANCH",
      "narration": "Postman example transaction",
      "clientReference": "string"
    }
order: 10000
