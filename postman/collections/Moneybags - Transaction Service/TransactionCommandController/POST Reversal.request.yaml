$kind: http-request
description: |-
  Generated from services/transaction-service/src/main/java/com/moneybags/transaction/controller/TransactionCommandController.java.reversal

  Request body type: ReversalRequest
url: "{{baseUrl}}/api/v1/transactions/{{id}}/reversals"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: Idempotency-Key
    value: "{{idempotencyKey}}"
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "reason": "Postman example"
    }
order: 14000
