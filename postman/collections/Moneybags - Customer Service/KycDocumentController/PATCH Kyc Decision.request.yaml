$kind: http-request
description: |-
  Generated from services/customer-service/src/main/java/com/moneybags/customer/controller/KycDocumentController.java.kycDecision

  Request body type: KycDecision
url: "{{baseUrl}}/api/v1/customers/{{cif}}/kyc-documents/{{documentId}}/decision"
method: PATCH
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "status": "VERIFIED",
      "employeeId": 1,
      "rejectionReason": "string"
    }
order: 3000
