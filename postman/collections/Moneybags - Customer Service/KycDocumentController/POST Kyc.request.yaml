$kind: http-request
description: |-
  Generated from services/customer-service/src/main/java/com/moneybags/customer/controller/KycDocumentController.java.kyc

  Request body type: KycSubmit
url: "{{baseUrl}}/api/v1/customers/{{cif}}/kyc-documents"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "docType": "PAN",
      "docNumber": "ABCDE1234F",
      "expiryDate": "2026-08-17",
      "filePath": "demo://kyc/pan"
    }
order: 1000
