$kind: http-request
description: |-
  Generated from services/customer-service/src/main/java/com/moneybags/customer/controller/CustomerController.java.create

  Request body type: CustomerRequest
url: "{{baseUrl}}/api/v1/customers"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "userId": 1,
      "firstName": "Asha",
      "lastName": "Sharma",
      "dob": "2026-08-17",
      "gender": "MALE",
      "mobile": "9876543210",
      "email": "asha@example.com",
      "panNo": "ABCDE1234F",
      "status": "ACTIVE",
      "kycStatus": "PENDING"
    }
order: 1000
