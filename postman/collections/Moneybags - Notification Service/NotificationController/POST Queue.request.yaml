$kind: http-request
description: |-
  Generated from services/notification-service/src/main/java/com/moneybags/notification/controller/NotificationController.java.queue

  Request body type: SendRequest
url: "{{baseUrl}}/api/v1/notifications"
method: POST
headers:
  Content-Type: application/json
  Idempotency-Key: "{{idempotencyKey}}"
  X-Permissions: "{{permissions}}"
body:
  type: json
  content: |-
    {
      "channel": "EMAIL",
      "recipient": "asha@example.com",
      "templateCode": "POSTMAN_TEMPLATE",
      "subject": "string",
      "body": "string",
      "cifNo": "CIF900101",
      "correlationId": "string",
      "variables": {
        "key": "value"
      }
    }
order: 1000
