$kind: http-request
description: |-
  Generated from services/notification-service/src/main/java/com/moneybags/notification/controller/NotificationController.java.createTemplate

  Request body type: TemplateRequest
url: "{{baseUrl}}/api/v1/notification-templates"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "templateCode": "POSTMAN_TEMPLATE",
      "channel": "EMAIL",
      "subjectTemplate": "Moneybags notification",
      "bodyTemplate": "Hello {{name}}, your request is complete.",
      "locale": "en-IN"
    }
order: 6000
