$kind: http-request
description: |-
  Generated from services/configuration-service/src/main/java/com/moneybags/configuration_service/controller/MaintenanceWindowController.java.update

  Request body type: UpdateMaintenanceWindowRequest
url: "{{baseUrl}}/api/v1/configuration/maintenance-windows/{{id}}"
method: PATCH
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "title": "string",
      "startsAt": "2026-08-17T10:00:00Z",
      "endsAt": "2026-08-17T10:00:00Z"
    }
order: 4000
