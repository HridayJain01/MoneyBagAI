$kind: http-request
description: |-
  Generated from services/configuration-service/src/main/java/com/moneybags/configuration_service/controller/MaintenanceWindowController.java.transitionStatus

  Request body type: UpdateStatusRequest
url: "{{baseUrl}}/api/v1/configuration/maintenance-windows/{{id}}/status"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Employee-Id
    value: "{{employeeId}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Branch-Code
    value: "{{branchCode}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Permissions
    value: "{{permissions}}"
    description: Used when calling a service directly; the gateway normally injects it.
  - key: X-Correlation-Id
    value: "{{correlationId}}"
body:
  type: json
  content: |-
    {
      "status": "ACTIVE"
    }
order: 5000
